.env
.env.*
*.env
!.env.example

# Build artifacts
/bin/
/dist/
data-catalog-mcp

# Local client config: contains machine-specific absolute paths.
client.json

# Go
*.test
*.out

# OS/editor cruft
.DS_Store
*.swp

# Raw source CSVs/zips are re-downloaded, not committed — only the
# converted Parquet files (data/*.parquet) are tracked.
data/*.csv
data/*.zip
